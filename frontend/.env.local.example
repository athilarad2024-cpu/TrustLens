# frontend/.env.local.example
# Copy this to .env.local and set the backend URL if it differs from the default.
# This file is safe to commit (no secrets). Do NOT put API keys here.

# Backend API base URL
VITE_API_BASE_URL=http://localhost:8000/api
